// Configuration de performance simplifiée
export const performanceOptimizations = {
  // Cache des composants pour éviter les re-rendus
  onPush: true,
  
  // Lazy loading agressif
  lazyModules: true,
  
  // Preload critique seulement
  preloadCritical: true
};