{
  "name": "pfa-production",
  "version": "1.0.1",
  "description": "PFA Application - Production Build",
  "main": "backend/src/index.js",
  "scripts": {
    "start": "cd backend && node src/index.js",
    "build": "cd frontend && npm ci --include=dev && npm run build --configuration production && cd ../backend && npm ci --production",
    "postbuild": "echo Build completed",
    "railway-start": "cd backend && node src/index.js"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "dependencies": {
    "axios": "^1.13.2"
  }
}
