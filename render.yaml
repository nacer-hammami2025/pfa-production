services:
  - type: web
    name: pfa-frontend
    env: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: frontend/dist/pfa-frontend
    routes:
      - type: rewrite
        source: /*
        destination: /index.html